#!/bin/sh
# port-env.sh — Party Hard GO — HOOK DO DONO / OWNER HOOK
#
# PT: Este arquivo é seu e nunca é sobrescrito por atualização. O padrão de
#     vídeo fica em NEXTOSSETTINGS.txt. Para substituir somente nesta camada,
#     remova o '#' da linha abaixo. Valores: auto|engine|preserve|stretch.
# EN: This file belongs to you and updates never overwrite it. The normal
#     video choice lives in NEXTOSSETTINGS.txt. To override it here, uncomment
#     the line below. Values: auto|engine|preserve|stretch.
#
# export NX_VIDEO_ASPECT=preserve
